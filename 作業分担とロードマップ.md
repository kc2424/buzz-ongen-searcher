# 作業分担とロードマップ

Claude Code を「ブレイン（設計・検証・修理）」、Base44 を「顔（表示・配布）」として使うための計画。

---

## 0. 調査で判明した2つの事実

### 事実1: Base44のバックエンド関数は月$40から

| プラン | 月額 | バックエンド関数 | GitHub連携 | カスタムドメイン |
|---|---|---|---|---|
| Free | $0 | ✗ | ✗ | ✗ |
| Starter | $16 | ✗ | ✗ | ✗ |
| **Builder** | **$40** | ✅ | ✅ | ✅ |

### 事実2: ランキングAPIはブラウザから直接呼べない（実測済み）

| データ元 | CORS | 意味 |
|---|---|---|
| Apple Music ランキングRSS | ヘッダなし ❌ | フロントから呼ぶと必ず失敗 |
| iTunes 検索・試聴 | 許可 ✅ | フロントから直接OK |
| raw.githubusercontent.com | `*` ✅ | **どこからでも読める** |

事実1と事実2を素直に組み合わせると「**月$40払わないとランキングが取れない**」となる。
これが最大の設計上の分岐点。

---

## 1. 回避策：GitHubを間に挟む

3つ目の事実 —— `raw.githubusercontent.com` が `Access-Control-Allow-Origin: *` を返す ——
を使うと、Base44のバックエンド関数が**丸ごと不要**になる。

```
 [ Claude Code ]  設計・収集スクリプト・解析ロジック
        │  git push
        ▼
 [ GitHub リポジトリ ]（publicなら無料・実行時間無制限）
        ├─ GitHub Actions : 毎日1回 収集スクリプトを自動実行 → JSONを更新
        └─ raw.githubusercontent.com : CORS付きでJSONを配信
        │  fetch（フロントから直接読める）
        ▼
 [ Base44 アプリ ]  表示・ログイン・スマホ対応・公開URL
```

この形にすると:

- ✅ 自分のPCが消えていても毎日データが貯まる（GitHub Actionsがクラウドで動く）
- ✅ Base44は無料〜Starterプランで足りる（バックエンド関数を使わないので）
- ✅ 解析ロジックがGitHubに残る＝Base44に依存しない。乗り換えも作り直しも自由
- ✅ Claude Codeがいつでもロジックを直せる（Base44の中は触りにくい）

---

## 2. 分担の判断基準

> **データと「正しさ」に関わるものは全部 Claude Code。**
> **人間の目に触れる部分だけ Base44。**

理由は、間違いが起きたときのコストが非対称だから。
見た目のズレは見ればわかるが、順位の計算間違いは**見てもわからない**。
だから計算はテストできる場所（Claude Code）に置く。

### それぞれの得意・不得意

| | Claude Code | Base44 |
|---|---|---|
| **得意** | 実データでの検証、アルゴリズム設計、バグの原因特定、何回試しても無料、ローカル資産との連携、Git管理 | 認証、公開URL、スマホ対応、見た目、5分で動くものが出る |
| **不得意** | PCが起動していないと動かない、公開URLを配れない、スマホから見られない、認証をゼロから作ると重い | 中身がブラックボックス、試行錯誤するたびクレジット消費、複雑なロジック、ベンダーロックイン |

**きれいに補完関係になっている。** 片方の不得意をもう片方が埋める。

---

## 3. 工程の仕分け

| # | 工程 | 担当 | 理由 |
|---|---|---|---|
| 1 | どのデータが取れるか調査 | **Claude Code** | 実際にAPIを叩いて確かめられる。済 |
| 2 | 「バズ」の定義（何位上がったら急上昇か） | **Claude Code** | 仕様の中核。テストで検証すべき |
| 3 | 収集スクリプト | **Claude Code** | ロジック本体。Gitに残す |
| 4 | 毎日の自動実行 | **GitHub Actions** | 無料・PCオフでも動く |
| 5 | 解析結果をJSONで公開 | **Claude Code** | Base44が読む形に整える |
| 6 | 画面デザイン・レイアウト | **Base44** | 見ながら直せるのが速い |
| 7 | ログイン／サインアップ | **Base44** | ここは本当にワンクリック。自作は無駄 |
| 8 | スマホ対応 | **Base44** | 標準で付いてくる |
| 9 | 公開・URL配布 | **Base44** | Claude Codeにはできない |
| 10 | バグが出たときの原因特定 | **Claude Code** | ログとデータを直接見て切り分けられる |
| 11 | Base44に渡す指示文の作成 | **Claude Code** | 制約（CORS等）を織り込んだ指示が書ける |

**Base44に渡す指示文をClaude Codeが書く**、が両者をつなぐ要。
Base44に直接思いつきで喋ると、制約を知らないまま作られてクレジットを溶かす。

---

## 4. 実装までの道のり

### Phase 0 ✅ 完了
ローカル版で「データが取れること」「急上昇ロジックが正しいこと」を確認済み。
以降のフェーズで迷ったときの**正解データ**として使う。

### Phase 1 — 収集をPCから切り離す ｜ Claude Code
- GitHubリポジトリを作る（public）
- 収集スクリプト `collect.py` を書く（`server.py` から収集部分だけ抜き出す）
- GitHub Actions のワークフローを書く（毎日 JST 朝に実行）
- **完了条件**: PCの電源を切った翌日、GitHub上のJSONが自動更新されている

### Phase 2 — 解析結果をJSONで公開 ｜ Claude Code
- 順位履歴から急上昇を計算し、Base44がそのまま表示できる形のJSONを吐く
- **完了条件**: `raw.githubusercontent.com/.../chart.json` をブラウザで開いて中身が見える

### Phase 3 — 画面を作る ｜ Base44（指示文はClaude Code）
- Claude Codeが「JSONのURL・フィールド名・画面仕様」を含む指示文を用意
- それをBase44に貼って生成 → 見た目は Base44 上で直接調整
- **完了条件**: ブラウザで3タブが動き、試聴とTikTok/Shortsリンクが機能する

### Phase 4 — ログインと公開 ｜ Base44
- 認証をON（Base44の標準機能）
- 公開してURLを取得、スマホで開く
- **完了条件**: 自分のスマホで、ログインして使える

### Phase 5 — 運用と修理 ｜ Claude Code
- 「急上昇の閾値が緩すぎる／厳しすぎる」等はスクリプト側を調整
- Base44側でエラーが出たら「期待と結果」の形で持ち込む

---

## 5. お金の話

| 項目 | 費用 |
|---|---|
| Claude Code | 既に使用中 |
| GitHub（public リポジトリ + Actions） | **$0** |
| Apple / iTunes API | **$0**（キー不要） |
| Base44 | 無料枠で開始。カスタムドメインが欲しくなったら検討 |

この構成の狙いは、**Base44に課金する判断を後回しにできること**。
先に中身（データと解析）を無料で完成させておけば、
Base44が合わなければ別のツールに乗り換えるだけで済む。

---

## 6. 次にやること

Phase 1。Claude Codeに「Phase 1を始めて」と言えば着手する。
GitHubアカウントが必要（無料）。まだなら先に作る。

---

## メモ：Base44を使うときの注意

- GitHub連携は**一度つなぐと解除できない**（Base44に戻せない）。使うなら覚悟の上で
- 同期されるのはフロントエンドのみ。Base44のバックエンドには依存し続ける
- だから**このロードマップではBase44のGitHub連携を使わない**。
  GitHubは自分のリポジトリとして独立させ、Base44からはURLで読むだけにする
